#!/usr/bin/env bash
# ABOUTME: Git pre-merge-commit hook that runs the full check set in fail-fast sprints on merges.
# ABOUTME: Sprints: fast static checks -> PHPStan -> tests; stops at the first failing sprint.

set -uo pipefail

# shellcheck source=.githooks/lib.sh
. "$(dirname "$0")/lib.sh"

full_checks
