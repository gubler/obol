#!/usr/bin/env bash
# ABOUTME: Git pre-push hook that runs the full check set in fail-fast sprints before the server round-trip.
# ABOUTME: Sprints: fast static checks -> PHPStan -> tests; stops at the first failing sprint. main is PR-only.

set -uo pipefail

# shellcheck source=.githooks/lib.sh
. "$(dirname "$0")/lib.sh"

full_checks
