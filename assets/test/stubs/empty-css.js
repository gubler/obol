// ABOUTME: Vitest resolution stub standing in for a vendored CSS import (e.g. driver.js/dist/driver.css).
// ABOUTME: CSS has no behavior under jsdom, so this resolves to an empty module.

export default {};
