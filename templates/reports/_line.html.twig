{# Obligation-trend line. Expects: chart (a ux-chartjs Chart). The obligation-trend controller formats axis and tooltip. #}
<div class="obligation-trend-chart relative h-72" data-controller="obligation-trend">
    {{ render_chart(chart) }}
</div>
