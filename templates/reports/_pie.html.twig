{# Composition pie. Expects: chart (a ux-chartjs Chart). The composition-pie controller adds native-amount tooltips. #}
<div class="reports-pie relative h-80" data-controller="composition-pie">
    {{ render_chart(chart) }}
</div>
