{# A single subscription tile. Expects: subscription (Subscription). #}
<a href="{{ path('subscription_show', {id: subscription.id}) }}"
   class="subscription-tile {{ subscription.color.gradientClasses() }} relative flex aspect-[5/3] flex-col justify-between overflow-hidden rounded-xl p-4 text-white shadow-sm transition hover:brightness-110">
    <div class="flex items-start justify-between gap-2">
        <div class="text-2xl font-bold leading-none">
            {% if subscription.logo %}
                <img src="{{ subscription.logo }}" alt="" class="size-8 object-contain">
            {% endif %}
        </div>
        <div class="text-xl font-bold leading-none"
             title="per {{ subscription.paymentPeriodCount > 1 ? subscription.paymentPeriodCount ~ ' ' : '' }}{{ subscription.paymentPeriod.value }}">
            ${{ (subscription.cost / 100)|number_format(2) }}
        </div>
    </div>
    <div>
        <div class="font-semibold leading-tight">{{ subscription.name }}</div>
        <div class="mt-0.5 text-sm text-white/80">{{ subscription.nextRenewal|time_diff }}</div>
        {% if subscription.archived %}
            <span class="archived-badge mt-2 inline-flex items-center rounded-full bg-black/30 px-2 py-0.5 text-xs font-medium text-white">Archived</span>
        {% endif %}
    </div>
</a>
